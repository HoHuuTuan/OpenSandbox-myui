services:
  opensandbox-server:
    image: opensandbox/server:latest
    container_name: opensandbox-server
    network_mode: bridge
    ports:
      - "8090:8090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    configs:
      - source: opensandbox-config
        target: /etc/opensandbox/config.toml
    environment:
      - SANDBOX_CONFIG_PATH=/etc/opensandbox/config.toml
  sdk-client:
    image: python:3.11-slim
    container_name: sdk-client
    command: >
      sh -c "pip install opensandbox && tail -f /dev/null"
    environment:
      - OPENSANDBOX_SERVER_URL=http://host.docker.internal:8090

  ai-server:
    build:
      context: ./ai-server
    container_name: ai-server
    networks:
      - opensandbox-net
    ports:
      - "3001:3001"
    env_file:
      - ./ai-server/.env
    restart: unless-stopped

  mock-source:
    build:
      context: ./mock-source
    container_name: mock-source
    networks:
      - opensandbox-net
    expose:
      - "3301"
    environment:
      - MOCK_SOURCE_PORT=3301
      - MOCK_SOURCE_API_KEY=source-secret
    restart: unless-stopped

  data-broker:
    build:
      context: ./data-broker
    container_name: data-broker
    depends_on:
      - mock-source
    networks:
      - opensandbox-net
    ports:
      - "3302:3302"
    environment:
      - DATA_BROKER_PORT=3302
      - DATA_BROKER_TOKEN=broker-secret
      - DATA_BROKER_SOURCE_BASE_URL=http://mock-source:3301
      - DATA_BROKER_SOURCE_TOKEN=source-secret
    restart: unless-stopped

configs:
  opensandbox-config:
    content: |
      [server]
      host = "0.0.0.0"
      port = 8090
      log_level = "INFO"

      [runtime]
      type = "docker"
      execd_image = "sandbox-registry.cn-zhangjiakou.cr.aliyuncs.com/opensandbox/execd:v1.0.9"

      [egress]
      image = "opensandbox/egress:v1.0.4"

      [docker]
      network_mode = "bridge"
      host_ip = "host.docker.internal"
      drop_capabilities = ["AUDIT_WRITE", "MKNOD", "SYS_ADMIN", "SYS_MODULE", "SYS_PTRACE", "SYS_TIME", "SYS_TTY_CONFIG"]
      no_new_privileges = false
      pids_limit = 4096

      [ingress]
      mode = "direct"

networks:
  opensandbox-net:
    driver: bridge
